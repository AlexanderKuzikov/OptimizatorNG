{
  "name": "optimizatorng",
  "version": "1.0.0",
  "description": "Десктопное приложение для оптимизации шаблонов документов.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && esbuild src/renderer.ts --bundle --outfile=dist/renderer.js --platform=browser",
    "start": "npm run build && electron .",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "electron": "^31.0.2",
    "electron-builder": "^24.13.3",
    "esbuild": "^0.25.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@types/adm-zip": "^0.5.7",
    "adm-zip": "^0.5.16"
  }
}
